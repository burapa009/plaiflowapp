# Phase 11 PromptPay staging gate

Status: Not run. On 2026-09-25 the Omise account was confirmed in test mode, test keys were present, and a test webhook secret was generated with the Owner's approval. No webhook endpoint, Railway secret, charge, migration, or staging deployment was configured. `BILLING_ENABLED` and `NEXT_PUBLIC_BILLING_ENABLED` must remain unset/false.

1. Validate migrations 13 and 14 on a disposable PostgreSQL database using `OCR_TEST_ADMIN_URL` and `go test ./internal/postgres -run TestPromptPayPeriodIsTenantScopedAndReplaySafe -v`. Confirm both `organization_plan_periods` and the new billing ledger preserve tenant isolation. Migration 14 is additive; never run its down migration after payment data exists.
2. Obtain an approved Omise **test** account and activate the PromptPay channel in test mode. Put `OMISE_SECRET_KEY` and the Base64 `OMISE_WEBHOOK_SECRET` only in the staging API service. Configure an HTTPS `POST /webhooks/omise` endpoint in Omise test mode. Do not place secrets in Vercel, `NEXT_PUBLIC_*`, logs, files, or the repository.
3. Deploy API and web with billing flags off; verify `/readyz` and the pricing page. Apply migration 14 only after the migration test and Phase 10 migration 13 readiness pass. Turn on `BILLING_ENABLED` for controlled staging tests, keeping public web navigation gated.
4. As a test Owner, create monthly, six-month, and annual QR charges. For each, verify exact THB amount, QR expiry, pending local rights, provider-confirmed payment, one paid period, correct paid-through date, and actual feature gate. Verify forged and duplicate webhooks, direct API role denial, Organization A/B isolation, tampered amount/Plan/interval, cancel/resume, missed webhook reconciliation, and provider outage behavior.
5. Before any live collection, implement and verify the remaining Phase 11 requirements: seller receipt issuance/email/history, paid Document carry and Scan Credits, scheduled Plan/interval changes and upgrades, renewal/failure reminders, measured p95/latency, firm gates, Thai accountant review, and Omise live account/PromptPay approval. Record a real rollback drill that turns both billing flags off without deleting payment history.

Sources: [Omise PromptPay](https://docs.omise.co/th/promptpay/thailand), [webhook verification](https://docs.omise.co/api-webhooks), [live account documents](https://docs.omise.co/th/how-do-i-enable-live-account/thailand).
