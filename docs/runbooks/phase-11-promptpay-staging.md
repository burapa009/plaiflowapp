# Phase 11 PromptPay staging gate

Status (2026-09-26): Omise test webhook points to `https://api-staging-217a.up.railway.app/webhooks/omise`; its test key and webhook secret are sealed Railway staging API variables. Staging database is at migration `15|f`; migration 15 adds Growth to paid-plan constraints without removing payment history. API deployment `5549f99a-92fa-4ece-b6ed-2a9c1ce8e0b3` is Online and `/readyz` returns 200. Vercel deployment `dpl_EwaWNTajco43nZPsAjT8BPhFZmmB` is READY at `https://plaiflowapp.vercel.app`. Public `/v1/plans` returns five packages and reports `billing_enabled=false`, `billing_test_mode=true`; all three `/pricing` intervals return 200 and show Growth and Accounting Firm with public checkout closed. `BILLING_ENABLED=true` only on staging API, with `BILLING_TEST_ORGANIZATION_ID=810b045f-a827-4ee0-a8a6-c48b99997614` (PlaiFlow Omise Test). The production Vercel site remains backed by the staging API. Migration 15 and the new Growth payment flow have not yet had a staging end-to-end payment test.

Controlled end-to-end result: The test Owner created a Starter monthly PromptPay QR for THB 150. Omise charge `chrg_test_6954rfd3zmljdkzjat0` was manually marked paid in test mode. The UI then showed Starter active through 2026-10-26; the database has one paid intent, one billing period, and one processed `charge.complete` event. A second THB 150 test charge `chrg_test_6954s1ihrss28hfe2z8` was manually marked failed. Its event was processed, the paid period count stayed one, and the Starter subscription remained active. No real payment occurred. The Go PostgreSQL integration test still could not connect from local CLI because there is no public database URL; its isolated test skipped without `OCR_TEST_ADMIN_URL`. The disposable database migration check and actual staging payment flows are separate evidence, not a substitute for that test.

1. Run `go test ./internal/postgres -run TestPromptPayPeriodIsTenantScopedAndReplaySafe -v` with a disposable PostgreSQL admin URL when one is available. The test creates and drops only its own database. Migration 14 is additive; never run its down migration after payment data exists.
2. Keep `OMISE_SECRET_KEY` and `OMISE_WEBHOOK_SECRET` only in the staging API service. Keep `BILLING_TEST_ORGANIZATION_ID` set to the selected test Organization UUID and public Pricing closed. Do not place secrets in Vercel, `NEXT_PUBLIC_*`, logs, files, or the repository.
3. Before widening the test, verify six-month and annual QR amounts and dates; forged and duplicate webhooks; direct API role denial; tampered amount, Plan, and interval; cancel/resume; missed webhook reconciliation; and provider outage behavior. Monthly paid, failed, and Organization A/B isolation have already been checked on staging.
4. Keep `NEXT_PUBLIC_BILLING_ENABLED` off and leave the staging API test allowlist in place until the remaining checks are complete.
5. Before any live collection, implement and verify the remaining Phase 11 requirements: seller receipt issuance/email/history, paid Document carry and Scan Credits, scheduled Plan/interval changes and upgrades, renewal/failure reminders, measured p95/latency, firm gates, Thai accountant review, and Omise live account/PromptPay approval. Record a real rollback drill that turns both billing flags off without deleting payment history.

Sources: [Omise PromptPay](https://docs.omise.co/th/promptpay/thailand), [webhook verification](https://docs.omise.co/api-webhooks), [live account documents](https://docs.omise.co/th/how-do-i-enable-live-account/thailand).
